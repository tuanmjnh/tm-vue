{
  "name": "tm-vue",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 8100",
    "preview": "vite preview --port 5050",
    "vue:cap": "vue add capacitor",
    "android:add": "npx cap add android",
    "android:copy": "npx cap copy android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "ios:add": "npx cap add ios",
    "ios:copy": "npx cap copy ios",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "build": "vite build",
    "cap:build": "vue-cli-service capacitor:build",
    "cap:serve": "vue-cli-service capacitor:serve",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "@capacitor/android": "^2.0.0",
    "@capacitor/cli": "^2.0.0",
    "@capacitor/core": "^2.0.0",
    "@capacitor/ios": "^2.0.0",
    "vue": "^3.2.33",
    "vue-router": "^4.0.14"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.1",
    "eslint": "^8.5.0",
    "eslint-plugin-vue": "^8.2.0",
    "vite": "^2.9.5",
    "vue-cli-plugin-capacitor": "~2.0.1"
  }
}
